<h2> All the articles </h2>
    <% @articles.each do |article| %>
    <p><%= article.title %></p>
    <p><%= article.content %></p>
    <p><a href="<%= "/articles/#{article.id}" %>"><%= article.title %></a>
    <% end %>